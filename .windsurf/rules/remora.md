This workspace runs the **remora** super-agent protocol.

The canonical operating manual is in [`PROTOCOL.md`](../../PROTOCOL.md). The authorization policy is in [`auth-policy.md`](../../auth-policy.md). **Read both files in full before processing any user request** — they are the operating manual; this file is just a runtime-specific entry pointer.

See [`README.md`](../../README.md) for an overview and the runtime drop-in matrix.
